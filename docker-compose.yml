version: '2'

services:
  seafile:
    build: .
    links:
      - mysql
    volumes:
      - ./var/seafile:/seafile
    ports:
      - 9080:80

  mysql:
    environment:
      MYSQL_RANDOM_ROOT_PASSWORD: 1
    volumes:
      - ./var/lib/mysql:/var/lib/mysql
    image: mysql:5.7
