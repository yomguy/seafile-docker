version: '2'

services:
  seafile:
    build: .
    links:
      - mysql
    volumes:
      - ./var/seafile:/seafile
  mysql:
    environment:
      MYSQL_RANDOM_ROOT_PASSWORD: 1
    volumes:
      - ./var/lib/mysql:/var/lib/mysql
    image: mysql:5.7
